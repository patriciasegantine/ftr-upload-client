import {UploadWidget} from "./components/widget/upload-widget.tsx";

export function App() {
    return (
        <main className="h-dvh flex flex-col items-center justify-center p-10">
            <UploadWidget />
        </main>
    );
}